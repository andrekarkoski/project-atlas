name: atlas_engine
resolution: workspace
description: Core engine for Project Atlas
version: 0.1.0

environment:
  sdk: ^3.12.2

dependencies:
  meta: ^1.12.0
  collection: ^1.18.0

dev_dependencies:
  test: ^1.25.0
