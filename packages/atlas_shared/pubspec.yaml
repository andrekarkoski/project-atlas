name: atlas_shared
resolution: workspace
description: A starting point for Dart libraries or applications.
version: 1.0.0

environment:
  sdk: ^3.12.2

dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6